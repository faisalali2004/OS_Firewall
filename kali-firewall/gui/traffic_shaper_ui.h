#pragma once
#include <QWidget>

class TrafficShaperUI : public QWidget {
    Q_OBJECT

public:
    explicit TrafficShaperUI(QWidget* parent = nullptr);
    ~TrafficShaperUI();
    // ... other members ...
};