#pragma once
#include <QWidget>

class RuleEditor : public QWidget {
    Q_OBJECT

public:
    explicit RuleEditor(QWidget* parent = nullptr);
    ~RuleEditor();
    // ... other members ...
};